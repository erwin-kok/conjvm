package org.erwinkok.conjvm.linking

import org.erwinkok.conjvm.parser.ErrorReporter
import org.erwinkok.conjvm.types.FunctionSymbol
import org.erwinkok.conjvm.types.VariableSymbol

class GlobalSymbolTable {
    fun addVariable(symbol: VariableSymbol) {
    }

    fun addFunction(symbol: FunctionSymbol) {
    }

    fun validate(reporter: ErrorReporter) {
    }
}
